{
  "name": "runrun-backend-proxy",
  "version": "1.0.0",
  "description": "Run Run Backend",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm ci --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
